# Runtime dependencies. Kept in sync with [project.dependencies] in pyproject.toml,
# which is the source of truth — this file exists for `pip install -r` workflows
# that don't want an editable package install.
hyperliquid-python-sdk>=0.4
websockets>=12.0
aiohttp>=3.9
pyyaml>=6.0
aiosqlite>=0.20
